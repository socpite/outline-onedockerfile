#!/bin/bash
# Test script for Outline CLI tools

echo "🧪 Testing Outline CLI Tools"
echo "============================="

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Test help commands
echo ""
echo "📋 Testing help commands..."

echo "1. Main CLI help:"
"$SCRIPT_DIR/outline-cli" help

echo ""
echo "2. Export help:"
"$SCRIPT_DIR/outline-export" --help

echo ""
echo "3. Import help:"
"$SCRIPT_DIR/outline-import" --help

echo ""
echo "✅ CLI tools are ready!"
echo ""
echo "Usage examples:"
echo "  # Export workspace"
echo "  outline-cli export -d /tmp/outline-backup"
echo ""
echo "  # Import workspace"  
echo "  outline-cli import -d /tmp/outline-backup --force"
echo ""
echo "  # Dry run import"
echo "  outline-cli import -d /tmp/outline-backup --dry-run"